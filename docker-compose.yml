version: '3'

services:
  php:
    build: ./config/php
    volumes:
      - ./:/var/www/html
    environment:
      - DB_HOST=mysql
      - DB_USER=demo
      - DB_PASS=password
      - DB_NAME=example
  
  apache:
    build: ./config/apache
    depends_on:
      - php
      - mysql
    ports:
      - '8080:80'
    volumes:
      - ./:/var/www/html
  
  mysql:
    build: ./config/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=rootpassword
      - MYSQL_USER=demo
      - MYSQL_PASSWORD=password
      - MYSQL_DATABASE=example